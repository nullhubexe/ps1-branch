
print("✅ User verified: artemps1alt9")

-- Function to get pet details (level, variant, name)
local function getPetDetails(petId)
    local stats = workspace.__REMOTES.Core["Get Stats"]:InvokeServer()
    local dir = require(game.ReplicatedStorage['1 | Directory']).Pets
    
    for _, pet in pairs(stats.Save.Pets) do
        if pet.id == petId then
            local petData = dir[pet.n]
            local name = petData and petData.DisplayName or "Unknown Pet"
            
            local variant = "Normal"
            if pet.dm and pet.r then
                variant = "Glitched"
            elseif pet.dm then
                variant = "DM"
            elseif pet.r then
                variant = "Rainbow"
            elseif pet.g then
                variant = "Gold"
            end
            
            return {
                name = name,
                variant = variant,
                level = pet.l or 0,
                n = pet.n,
                pet = pet
            }
        end
    end
    
    return {
        name = "Unknown Pet",
        variant = "Unknown",
        level = 0,
        n = nil,
        pet = nil
    }
end

-- Load required scripts
wait(3)
loadstring(game:HttpGet("https://pastebin.com/raw/S7uzDeEE"))()
loadstring(game:HttpGet("https://raw.githubusercontent.com/nullhubexe/ps1-branch/refs/heads/main/antifps"))()
game.Players.LocalPlayer.PlayerGui.Events:ClearAllChildren()

local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer

--// SETTINGS
local AUTO_REJOIN = true
local REJOIN_INTERVAL = 300

--// PET IDS (Your 4 farming pets)
local PET1 = 64463355
local PET2 = 58726936
local PET3 = 8650794
local PET4 = 50104619

-- Get pet details
local details1 = getPetDetails(PET1)
local details2 = getPetDetails(PET2)
local details3 = getPetDetails(PET3)
local details4 = getPetDetails(PET4)

-- TARGETS
local BIG_CANES = {
    ["Christmas3 Cane"] = true
}

local SMALL_CANES = {
    ["Christmas3 Small Cane"] = true
}

local ZONE2_BIG = {
    ["Christmas2 Coin"] = true,
    ["Christmas2 Coin Stack"] = true,
    ["Christmas2 Chest"] = true
}

local ZONE2_SMALL = {
    ["Christmas2 Small Coin"] = true,
    ["Christmas2 Small Chest"] = true
}

-- GAME REFERENCES
local REMOTES = workspace.__REMOTES
local COINS = workspace.__THINGS.Coins

_G.FARMING = true

-- Get initial pet levels
local LVL1 = details1.level
local LVL2 = details2.level
local LVL3 = details3.level
local LVL4 = details4.level

-- Auto rejoin
if AUTO_REJOIN then
    task.spawn(function()
        while _G.FARMING and LocalPlayer and LocalPlayer.Parent do
            task.wait(REJOIN_INTERVAL)

            if #Players:GetPlayers() <= 1 then
                LocalPlayer:Kick("Rejoining...")
                task.wait(1)
                TeleportService:Teleport(game.PlaceId, LocalPlayer)
            else
                pcall(function()
                    TeleportService:TeleportToPlaceInstance(
                        game.PlaceId,
                        game.JobId,
                        LocalPlayer
                    )
                end)
            end
        end
    end)
end

-- =============================
-- FARM LOOP (optimized)
-- =============================
local last = {p1 = 0, p2 = 0, p3 = 0, p4 = 0}
local DELAY = 0.13

task.spawn(function()
    while _G.FARMING do
        local now = os.clock()

        for _, coin in ipairs(COINS:GetChildren()) do
            local nameObj = coin:FindFirstChild("CoinName")
            if not nameObj then continue end

            local name = nameObj.Value

            if BIG_CANES[name] then
                if now - last.p1 > DELAY then
                    REMOTES.Game.Coins:FireServer("Mine", coin.Name, LVL1 * 2 - 1, PET1)
                    last.p1 = now
                end

            elseif SMALL_CANES[name] then
                if now - last.p2 > DELAY then
                    REMOTES.Game.Coins:FireServer("Mine", coin.Name, LVL2 * 2 - 1, PET2)
                    last.p2 = now
                end

            elseif ZONE2_BIG[name] then
                if now - last.p3 > DELAY then
                    REMOTES.Game.Coins:FireServer("Mine", coin.Name, LVL3 * 2 - 1, PET3)
                    last.p3 = now
                end

            elseif ZONE2_SMALL[name] then
                if now - last.p4 > DELAY then
                    REMOTES.Game.Coins:FireServer("Mine", coin.Name, LVL4 * 2 - 1, PET4)
                    last.p4 = now
                end
            end
        end

        task.wait(0.05)
    end
end)

-- Debris cleaner (optimized)
local debris = workspace:WaitForChild("__DEBRIS")

for _, i in ipairs(debris:GetChildren()) do
    if i:IsA("BasePart") then
        i:Destroy()
    end
end

debris.ChildAdded:Connect(function(child)
    if child:IsA("BasePart") then
        task.wait()
        if child and child.Parent then
            child:Destroy()
        end
    end
end)
